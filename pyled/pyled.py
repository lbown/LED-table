import sys
import settings

# TODO: import correct movie driver based on config setting

def main(*args):
	
	# Specifies behavior to follow once pictureStack is exhausted
	queueDoneAction = None # TODO: read queue done action from config


	movieQueue = [] # TODO: read 
	pictureQueue = []

	# Main loop: execute every 1/60 second
		# Check input sources and update queueDoneAction
		# If len(pictureStack) == 1


if __name__ == '__main__':
	main(*sys.argv[1:])
	print setings.DEFAULT_MOVEIS