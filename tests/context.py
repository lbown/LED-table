import os
import sys
# Give python the correct import context
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

# Import code to be tested so it is shared with test modules
